#include <stdio.h>
int lemma(char w)
{
    char*p=w;
    int l=0;
    while(*p!='\0')
    {
        l++;
        p++;
    }
    if(l>=4 &&*(p-3)=='i'&&*(p-2)=='n'&&*(p-1)=='g')
    {
        *(p-3)='\0';
    }
     else if(l>=4 && *(p-3)=='e' && *(p-2)=='r' && *(p-1)=='s')
     {
        *(p-3)='\0';
     }
     else if(l>=3 && *(p-2)=='e' && *(p-1)=='s')
     {
        *(p-2)='\0';
     }
     else if(l>=3&& *(p-2)=='e' &&*(p-1)=='s')
     {
        *(p-2)='\0';
     }
     else if (l>=2 && *(p-1)=='s')
     {
        *(p-1)='\0'
     }
     return 0;
}
int main()
{
    char s[100];
    lemma (s);
    printf("%s",s);
    return 0;
}