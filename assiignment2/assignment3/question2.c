#include <stdio.h>
int main()
{
int i,a,n,sum=0;
printf("enter the number of elements\n");
a=(int)malloc(sizeof(n));
for(i=0;i<n;i++)
{
    scanf("%d"&a[i]);
    sum+=a[i];

}
printf("sum=%d",sum);
return 0;
}
